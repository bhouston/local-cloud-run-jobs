{
  "name": "local-cloud-run-jobs",
  "version": "1.0.0",
  "description": "A local API-compatible library for Google Cloud Run Jobs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/run": "^1.4.0"
  }
}
